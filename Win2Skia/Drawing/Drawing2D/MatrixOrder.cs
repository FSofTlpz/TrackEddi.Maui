namespace System.Drawing.Drawing2D {
   public enum MatrixOrder {
      Append = 1,  // Die neue Transformation wird nach der alten angewendet.
      Prepend = 0, // Die neue Transformation wird vor der alten angewendet.
   }
}
